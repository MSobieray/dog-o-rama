/** As the site and each page begins getting more content
 * we can use this pattern to easily add new content without
 * having to manually add the each section so the homepage but
 * we can import them all and loop through each section
 */

export { default as HeroSwap } from "./hero-swap"
export { default as BreedList } from "./breed-list"
